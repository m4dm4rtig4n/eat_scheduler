apiVersion: v1
kind: Service
metadata:
  name: {{ include "eat-scheduler.fullname" . }}
  labels:
    {{- include "eat-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eat-scheduler.selectorLabels" . | nindent 4 }}
